# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'fame' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fame
  pod 'Koloda'
  pod 'PureLayout'

  target 'fameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
